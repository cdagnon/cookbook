<%= form_for(@login) do |f| %>
  <% if @login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% @login.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :username, 'Login' %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password, :value => '' %>
    <%= f.hidden_field :encrypted_pwd %>
    <%= f.hidden_field :salt %>
  </div>
  <div class="field">
    <%= f.label :confirm_password %><br />
    <%= f.text_field :confirm_password, :value => '' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
